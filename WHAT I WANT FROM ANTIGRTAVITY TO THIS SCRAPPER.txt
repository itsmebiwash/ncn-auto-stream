The GitHub Action log and Facebook output reveal three specific bugs causing the pipeline to fail and freeze:

Python Module Collision (ModuleNotFoundError): Python has a built-in module named queue. Having a folder named queue/ in the project root causes from queue.worker import ... to fail because Python inspects its internal library instead of your folder.

Hanging GitHub Action Runner: Executing background processes like python run_scraper.py & in GitHub Actions keeps stdout hooks open, causing the job to run indefinitely until it hits a timeout.

Pexels Keyword Mismatch: Pexels returns generic typewriter photos because the search parameter passes broad terms like "news" instead of contextual English keywords like "traffic police driving license nepal".

Below is the diagnosis of your execution log, followed by PART 1 of the modular prompt sequence for Antigravity.

Error Analysis from Screenshot Logs
Plaintext
ModuleNotFoundError: No module named 'queue.worker'; 'queue' is not a package
Cause: Name collision with Python's standard queue module.

Fix: Rename folder queue/ to job_queue/.

Plaintext
Failed to initialize MongoDB: MONGO_URL is not set in environment.
Cause: The worker script expects MONGO_URL in environment variables. If missing, it crashes or hangs silently without a fallback.

Fix: Add an automatic SQLite fallback (news_database.db) so the pipeline operates locally even if MongoDB environment variables are missing.

Implementation Strategy: Step-by-Step Prompt Plan
To prevent Antigravity from hallucinating, we break the execution into 4 distinct phases:

PART 1 (Current): Directory Structure Fixes, Module Renaming, Local Hash/History Database, and Environment Safety.

PART 2: Scraper Logic, Duplicate/Ad Filtering, Groq Key Rotation, and SEO Text Engine.

PART 3: Gemini Vision Filtering, Contextual Pexels Fetcher, and html2image Card Rendering.

PART 4: Facebook Graph API Publisher (4-minute timer, Image + Reel posting, Meta rate limits, GitHub Actions exit fix).

PART 1 PROMPT FOR ANTIGRAVITY
Copy and paste the prompt block below into Antigravity:

Markdown
# TASK: PART 1 - CORE ARCHITECTURE, DIRECTORY REFACTOR, & DATABASE HISTORY ENGINE

## OBJECTIVE
Fix critical Python module import collisions, resolve environment variable missing errors, and construct a robust deduplication history database.

---

## 1. DIRECTORY REFACTORING (CRITICAL FIX)
- Rename the root directory `queue/` to `job_queue/` to resolve collision with Python's standard library `queue`.
- Update all project imports across all `.py` files from `from queue.worker ...` to `from job_queue.worker ...`.
- Ensure all module directories contain `__init__.py`.

New Required Directory Structure:
├── config/
│   ├── __init__.py
│   └── settings.py
├── database/
│   ├── __init__.py
│   └── db_client.py
├── scrapers/
│   ├── __init__.py
│   └── nepali_scraper.py
├── ai/
│   ├── __init__.py
│   ├── groq_manager.py
│   └── gemini_vision.py
├── templates/
│   └── card_template.html
├── job_queue/
│   ├── __init__.py
│   └── worker.py
├── facebook/
│   ├── __init__.py
│   └── publisher.py
├── output/
├── logs/
└── run_pipeline.py

---

## 2. DATABASE CLIENT & HISTORY ENGINE (`database/db_client.py`)
Build a unified database layer that automatically defaults to a local SQLite database (`database/news_history.db`) if `MONGO_URL` is not provided in environment variables.

### History Tracking Requirements:
1. Schema / Table `articles`:
   - `id` (PRIMARY KEY / Hash)
   - `url` (TEXT)
   - `title_nepali` (TEXT)
   - `title_hash` (MD5 of normalized title string)
   - `scraped_at` (TIMESTAMP)
   - `status` (ENUM: 'scraped', 'text_processed', 'filtered_out', 'queued', 'posted', 'failed')
   - `priority_score` (FLOAT)
   - `json_data` (TEXT/JSON payload)
   - `posted_at` (TIMESTAMP)

2. History Check Logic:
   - Function `is_duplicate_history(url, title_nepali, lookback_hours=48)`:
     - Compute SHA256/MD5 hash of URL and cleaned title.
     - Query DB: If hash exists OR exact/similar title was logged in the past 48 hours, return `True`.
   - Function `mark_posted(article_id, fb_post_id)`:
     - Update status to `'posted'`, record timestamp, move generated asset references.

---

## 3. CONFIGURATION & ENVIRONMENT (`config/settings.py`)
- Safely parse environment variables without throwing unhandled exceptions.
- Parse comma-separated keys (`GROQ_API_KEYS`, `GEMINI_API_KEYS`) using string trimming `.strip()` to prevent whitespace auth errors.
- Ensure `OUTPUT_DIR` (`./output`) and `LOG_DIR` (`./logs`) are created automatically on initialization.

---

## REQUIRED DELIVERABLES FOR PART 1
1. Refactored `job_queue/` folder and fixed project-wide import paths.
2. Complete `database/db_client.py` with dual MongoDB / SQLite fallback.
3. Updated `config/settings.py` for safe environment parsing.
4. Verify no `ModuleNotFoundError` occurs when running `python -c "import job_queue.worker"`.










# TASK: PART 2 - GROQ 13-KEY MANAGER, SCRAPER, CROSS-SITE DEDUPLICATION, & SEO TEXT ENGINE

## OBJECTIVE
Build the multi-source scraper, cross-site deduplication history engine, 13-key Groq manager (Text + Vision), ad/spam filter, and SEO caption generator. Completely eliminate Gemini API references.

---

## 1. 13-KEY GROQ AI MANAGER (`ai/groq_manager.py`)

Create a unified AI engine that handles both text processing and vision checks using only the 13 Groq keys provided in `GROQ_API_KEYS`.

### Key Rotation & Failure Recovery:
- Parse `GROQ_API_KEYS` from environment variables into a list of 13 strings.
- Maintain a stateful rotation pool: `[{"key": k, "blocked_until": 0}]`.
- Round-robin key selection algorithm: Skip keys where `current_time < blocked_until`.
- On `HTTP 429` (Rate Limit) or network error:
  - Extract `retry-after` header value (or default to 30 seconds).
  - Mark key as blocked: `blocked_until = current_time + cooldown`.
  - Failover immediately to the next available key in the pool.

### Models to Use:
- **Text Tasks (Translation, Ad-Filter, SEO, Keywords):** `llama-3.1-8b-instant`
- **Vision Tasks (Image Quality & Logo Verification):** `llama-3.2-11b-vision-preview`

---

## 2. SCRAPER & CROSS-SITE DEDUPLICATION (`scrapers/nepali_scraper.py`)

Scrape news articles from 50+ Nepali targets (e.g., OnlineKhabar, Setopati, Ratopati, Annapurna Post).

### Deep History & Cross-Site Deduplication Rules:
Before making any AI calls:
1. Extract article URL, headline, body text, publication time, and main featured image URL.
2. Check `database/db_client.py` history logs for the past 48 hours:
   - **Exact Hash Match:** Match URL or MD5 hash of headline.
   - **Cross-Site Core Story Match:** Compare new title against existing titles from the past 8–9 hours across all scraped sites. If the core news event is identical (e.g., same political announcement or accident covered by multiple sites), flag as duplicate and discard immediately.

---

## 3. GROQ AD-FILTER & SEO TEXT ENGINE (`ai/groq_manager.py`)

Send valid Nepali news items to Groq `llama-3.1-8b-instant` using **Strict JSON Mode**.

### Groq System Prompt:
```text
You are a senior news editor and social media viral growth strategist. 
Analyze the following Nepali news article. 

Your tasks:
1. Identify if this is a real news story or an advertisement, sponsored post, self-promotion, or non-news notice.
2. Evaluate virality potential and timeliness (1 to 10 scale).
3. Translate and rewrite into viral English and concise Nepali content.
4. Extract SPECIFIC contextual search keywords for image fetching (DO NOT use generic terms like "news", "typewriter", "paper", or "press").

Respond ONLY with this JSON structure:
{
  "is_advertisement_or_promo": boolean,
  "is_duplicate_story": boolean,
  "virality_score": number,
  "card_headline_nepali": "string (5-8 words max, short & punchy for news card)",
  "card_subtitle_nepali": "string (10-12 words highlight summary)",
  "fb_caption_text": "string (SEO-optimized post body with key facts, engaging tone, and source credit)",
  "hashtags": ["#NepalCentralNews", "#Nepal", "#CategoryHashtag"],
  "pexels_search_keywords": ["specific context 1", "specific context 2"] 
}
Contextual Keyword Enforcement Rule:
BAD Keywords (Prohibited): ["news", "newspaper", "typewriter", "media", "breaking news"]

GOOD Keywords (Mandatory): For an article about driving licenses, return ["nepal traffic police", "driving license document", "kathmandu road"].

4. GATEWAY DECISION LOGIC
Process the JSON response from Groq:

If is_advertisement_or_promo == true OR is_duplicate_story == true:

Log reason in logs/pipeline.log.

Update DB status to filtered_out.

Terminate processing for this article immediately.

If article is valid:

Save generated headlines, captions, and contextual Pexels keywords to the DB record.

Advance article to Module 3 (Image & Card Renderer).

REQUIRED DELIVERABLES FOR PART 2
ai/groq_manager.py implementing 13-key rotation, exponential backoff, text generation, and vision model routing.

scrapers/nepali_scraper.py with 8–9 hour cross-site core story deduplication logic.

Clean error-handling tests ensuring zero execution halts if individual Groq keys return rate-limit codes.






# TASK: PART 3 - GROQ VISION CHECK, PEXELS CONTEXTUAL FETCHER, HTML2IMAGE CARD RENDERER, & IMAGE COMPRESSION

## OBJECTIVE
Implement the image validation engine using Groq Vision, the contextual Pexels API fallback, the HTML2Image card renderer (with headline, category badge, subtitle, and logo overlay), and the image compression pipeline.

---

## 1. GROQ VISION IMAGE INSPECTION (`ai/groq_manager.py`)

Add function `verify_image_with_groq_vision(image_path_or_url)` using model `llama-3.2-11b-vision-preview` and your 13-key Groq rotation pool.

### Inspection Task:
Examine the original image scraped from the news website to determine if it is usable editorial content or unwanted site assets.

### System Prompt & JSON Output:
```text
Analyze this news website image.
Determine if this image is a genuine editorial photo of a news event/person/location, OR if it is a website logo, stock promotional banner, advertisement, or broken graphic.

Respond ONLY with this JSON structure:
{
  "is_faulty_or_ad_image": boolean,
  "reason": "string"
}
2. PEXELS CONTEXTUAL FETCHER (utils/pexels_client.py)
Create a dedicated client that uses PEXELS_API_KEY to search for high-resolution background assets when the original website image is flagged as faulty.

Implementation Rules:
Receive pexels_search_keywords array generated by Groq in Part 2 (e.g., ["nepal traffic police", "kathmandu road"]).

Do not use fallback generic terms like "news", "typewriter", or "newspaper".

Query Pexels API (https://api.pexels.com/v1/search?query=...&orientation=landscape&per_page=3).

Select the highest resolution orientation matching a 16:9 or 3:2 landscape aspect ratio.

If Pexels yields 0 results, fall back to a styled solid dark/gradient canvas in the card renderer.

3. HTML2IMAGE CARD RENDERER & TEMPLATE (utils/card_renderer.py & templates/card_template.html)
Re-build the card renderer module using html2image to convert structured HTML/CSS templates into 1200x630 Facebook news cards.

Template Specifications (templates/card_template.html):
Canvas Dimensions: 1200px width x 630px height (Meta feed standard).

Visual Layers:

Background: Full-bleed background image (original verified news image or Pexels image) with a dark gradient overlay at the bottom 60% for contrast.

Top Overlay: Organization Brand Logo (from ./assets/logo.png) anchored in the top-left or top-right corner.

Topic Badge: Bright red/orange rounded pill badge with white text containing card_headline_nepali category (e.g., BREAKING NEWS | TIBET / LOCAL).

Headline: Large, high-legibility bold Nepali font (using Noto Sans Devanagari or Google Font imports) showing card_headline_nepali.

Subtitle / Highlight: Smaller 2-line Nepali highlight text (card_subtitle_nepali).

Footer Tag: Clean branding text (e.g., NEPAL CENTRAL NEWS).

Python Card Renderer Logic (utils/card_renderer.py):
Python
from html2image import Html2Image

def generate_news_card(background_image_path, headline_nepali, subtitle_nepali, output_file_path):
    """
    Renders card_template.html using provided metadata and outputs a PNG to output_file_path.
    Configures headless Chrome flags (--no-sandbox, --disable-gpu, --disable-dev-shm-usage)
    to prevent hanging in headless container/server environments.
    """
4. IMAGE OPTIMIZATION & FOLDER ROUTING (utils/image_processor.py)
Before queuing images for Facebook publishing, compress and optimize all output files using Pillow (PIL).

Processing Pipeline:
Resize: Ensure final dimensions are strictly 1200 x 630 pixels.

Compress: Convert to JPEG or WebP format with quality set to 85%. Enforce a maximum file size limit of 500 KB to eliminate Facebook Graph API upload timeouts.

Directory Lifecycle Management:

Save processed news cards ready for posting into ./output/ready/.

Track generated image file paths in the database record (image_path).

Create a helper function move_to_posted(image_path) that transfers published images from ./output/ready/ to ./output/posted/ once Facebook execution succeeds in Part 4.

REQUIRED DELIVERABLES FOR PART 3
Updated ai/groq_manager.py with Groq Vision function using llama-3.2-11b-vision-preview.

utils/pexels_client.py executing targeted keyword searches without fallback to typewriter stock photos.

templates/card_template.html and utils/card_renderer.py correctly rendering 1200x630 HTML-to-image cards.

utils/image_processor.py handling JPEG/WebP compression (<500 KB) and ./output/ready/ to ./output/posted/ directory transfers.






# TASK: PART 4 - FACEBOOK PUBLISHER, 4-MINUTE QUEUE WORKER, META RATE GUARD, & GITHUB ACTIONS FIX

## OBJECTIVE
Implement the Facebook Graph API publishing engine, 4-minute interval worker with atomic DB locking, Meta header usage parser, posted asset file moving, engagement telemetry, and fix the hanging GitHub Actions runner bug.

---

## 1. FACEBOOK GRAPH API PUBLISHER & RATE GUARD (`facebook/publisher.py`)

Create `facebook/publisher.py` to handle publishing to Facebook using `FB_PAGE_ID` and `FB_PAGE_ACCESS_TOKEN`.

### Publishing Logic:
- Endpoint: `https://graph.facebook.com/v21.0/{FB_PAGE_ID}/photos`
- Post Payload:
  - `source`: Open binary stream of optimized image file from `./output/ready/`.
  - `message`: `fb_caption_text` + double newline + `hashtags` + source attribution string.

### Rate Limit & Header Inspection:
- Read response headers `X-App-Usage` and `X-Page-Usage`.
- If `call_count` or `total_cputime` in parsed JSON headers exceeds **80%**:
  - Log `[RATE WARNING] Meta quota near ceiling. Worker pausing.`
  - Return rate warning status to delay next queue processing by 15 minutes.
- Error Handling:
  - If Meta returns `HTTP 429` or error code `368` (Spam Block):
    - Re-queue item status back to `queued`.
    - Enforce a 30-minute pause before any further Meta API attempts.

---

## 2. QUEUE WORKER & DOUBLE-POSTING PREVENTION (`job_queue/worker.py`)

Implement the background publishing loop with strict atomic locking to eliminate double-posting.

### Atomic Lock & Execution Steps:
1. Fetch the single highest-priority pending post:
   `SELECT * FROM articles WHERE status = 'queued' ORDER BY priority_score DESC, scraped_at DESC LIMIT 1;`
2. **Atomic Lock:** Immediately update status to `'processing'` in a single transaction:
   `UPDATE articles SET status = 'processing' WHERE id = ? AND status = 'queued';`
   - If rows affected == 0, abort (another process handled it).
3. Execute `facebook/publisher.py`.
4. On Success:
   - Update DB status to `'posted'`, save `fb_post_id`, and record `posted_at` timestamp.
   - Call `utils.image_processor.move_to_posted(image_path)` to transfer the file from `./output/ready/` to `./output/posted/`.
5. On Failure:
   - Mark status as `'failed'` (or re-queue if temporary network error).

### Posting Schedule:
- Maintain a strict 240-second (4-minute) delay between post executions when running continuously.

---

## 3. GITHUB ACTIONS RUNNER FIX (`run_pipeline.py` & `.github/workflows/scraper.yml`)

Fix the bug causing GitHub Action jobs to stay stuck for 2+ hours due to non-blocking background process calls (`python run_scraper.py &`).

### Execution Design for CI/CD:
In GitHub Actions, background loops like `while True: sleep(240)` keep stdout open and cause runner timeouts.

Modify `run_pipeline.py`:
- Accept an optional flag `--batch-mode`.
- When `--batch-mode` is enabled (for GitHub Actions):
  1. Execute hourly news scraper & deduplication.
  2. Run Groq text engine & ad filter.
  3. Run Groq vision check, Pexels fallback, and HTML2Image renderer.
  4. Post the top N queued items sequentially with explicit 240-second sleeps between each post.
  5. Close all DB connections and exit cleanly (`sys.exit(0)`).

### Updated GitHub Action Workflow `.github/workflows/scraper.yml`:
```yaml
name: Hourly News Scraper & Publisher

on:
  schedule:
    - cron: '0 * * * *' # Runs every hour
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install System Dependencies (Chromium for html2image)
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser
      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run Pipeline (Batch Mode)
        env:
          GROQ_API_KEYS: ${{ secrets.GROQ_API_KEYS }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: python run_pipeline.py --batch-mode
4. TELEMETRY & ENGAGEMENT TRACKER (facebook/insights.py)
Create facebook/insights.py to query Facebook post engagement metrics every 6 hours.

Query https://graph.facebook.com/v21.0/{fb_post_id}/insights?metric=post_impressions,post_engagements.

Calculate topic performance: If articles containing specific keywords achieve 50%+ higher engagement than the baseline, write a temporary weight modifier (+2.0) to config/settings.py or DB config for those keywords to boost future virality scoring in Part 2.

REQUIRED DELIVERABLES FOR PART 4
facebook/publisher.py with Meta header parsing and spam rate limit protection.

job_queue/worker.py with atomic SQL status locking to stop double posts.

Clean run_pipeline.py with --batch-mode flag and fixed .github/workflows/scraper.yml that exits cleanly after execution without hanging.

facebook/insights.py for post performance monitoring.



# TASK: PART 5 - AUTOMATED REELS GENERATOR, FACEBOOK REELS PUBLISHER, & FULL PIPELINE TEST

## OBJECTIVE
Implement the 9:16 vertical video reel generator (`utils/reel_generator.py`), the Facebook Reels upload engine (`facebook/reels_publisher.py`), update the 4-minute worker to publish both Image Post AND Reel for every top news item, and create an end-to-end dry-run verification script.

---

## 1. AUTOMATED REEL GENERATOR (`utils/reel_generator.py`)

Create a lightweight video rendering script using `moviepy`, `PIL`, and `ffmpeg` to generate 9:16 vertical video reels (1080x1920 pixels) from news card assets.

### Video Specifications:
- **Dimensions:** 1080 x 1920 pixels (9:16 Aspect Ratio).
- **Duration:** 10 to 12 seconds.
- **Visual Structure:**
  1. Background: News image with a slow zoom-in motion effect (Ken Burns effect).
  2. Dark Overlay: Semi-transparent gradient box at the center/bottom for high contrast text readability.
  3. Header & Logo: `NEPAL CENTRAL NEWS` logo overlay at top center.
  4. Animated Text: Highlight headline in bold Nepali text (`card_headline_nepali`), followed by a 2-line summary (`card_subtitle_nepali`).
- **Audio:** Include a clean background news bed track from `./assets/news_beat.mp3` trimmed to video length with audio fade-out.
- **Output:** Render H.264 video with AAC audio saved to `./output/ready_reels/{article_id}.mp4`.

---

## 2. FACEBOOK REELS PUBLISHER (`facebook/reels_publisher.py`)

Implement Meta's 3-step Facebook Page Video Reels API publishing flow using `FB_PAGE_ID` and `FB_PAGE_ACCESS_TOKEN`.

### Meta Video Reel Upload Workflow:
1. **Initialize Upload Session:**
   - `POST https://graph.facebook.com/v21.0/{FB_PAGE_ID}/video_reels`
   - Params: `upload_phase=start`, `access_token`
   - Extract: `video_id` and `upload_url`.
2. **Transfer Video Binary:**
   - `POST {upload_url}`
   - Headers: `Authorization: OAuth {FB_PAGE_ACCESS_TOKEN}`, `file_offset: 0`
   - Body: Raw MP4 bytes from `./output/ready_reels/`.
3. **Finish & Publish Reel:**
   - `POST https://graph.facebook.com/v21.0/{FB_PAGE_ID}/video_reels`
   - Params: `upload_phase=finish`, `video_id`, `video_state=PUBLISHED`, `description=fb_caption_text + hashtags`, `access_token`.

---

## 3. UPDATED QUEUE WORKER LOGIC (`job_queue/worker.py`)

Update the 4-minute worker to execute dual-publishing for every top news item:

1. Every 240 seconds (4 minutes), select the top pending news item from DB.
2. Publish Image News Card to Facebook Page Feed via `facebook/publisher.py`.
3. Publish Video Reel to Facebook Page Reels via `facebook/reels_publisher.py`.
4. On dual success:
   - Mark DB status as `'posted'`.
   - Transfer image card to `./output/posted/`.
   - Transfer reel file to `./output/posted_reels/`.

---

## 4. SYSTEM TEST SCRIPT (`test_entire_system.py`)

Create a master testing script to verify all 5 parts in a single dry run.

```python
"""
End-to-End Pipeline Dry-Run Test Script
"""
import sys
from config.settings import parse_env
from database.db_client import init_db, is_duplicate_history
from ai.groq_manager import process_text_groq, verify_image_groq
from utils.card_renderer import generate_news_card
from utils.reel_generator import generate_news_reel

def run_dry_run_test():
    print("=== STARTING END-TO-END SYSTEM TEST ===")
    
    # 1. Check Configuration & Keys
    cfg = parse_env()
    print(f"[OK] Configuration loaded. Found {len(cfg['GROQ_KEYS'])} Groq keys.")
    
    # 2. Test DB Initialization
    db = init_db()
    print("[OK] Database connection established.")
    
    # 3. Test Text AI Engine (Groq Text)
    sample_nepali_text = "सवारी चालक अनुमतिपत्र (लाइसेन्स) को विवरणमा त्रुटि भएमा सच्याउने तरिका।"
    ai_result = process_text_groq(sample_nepali_text, title="लाइसेन्स सम्बन्धी सूचना")
    print(f"[OK] Groq Text Response: Headline = '{ai_result.get('card_headline_nepali')}'")
    
    # 4. Test Card Renderer
    card_path = generate_news_card(
        bg_image="./assets/sample_bg.jpg",
        headline=ai_result['card_headline_nepali'],
        subtitle=ai_result['card_subtitle_nepali'],
        output_path="./output/ready/test_card.jpg"
    )
    print(f"[OK] Image Card generated at: {card_path}")
    
    # 5. Test Reel Generator
    reel_path = generate_news_reel(
        image_card_path=card_path,
        headline=ai_result['card_headline_nepali'],
        output_path="./output/ready_reels/test_reel.mp4"
    )
    print(f"[OK] Video Reel generated at: {reel_path}")
    
    print("=== ALL PIPELINE MODULES PASSED SANITY CHECKS ===")

if __name__ == "__main__":
    run_dry_run_test()
REQUIRED DELIVERABLES FOR PART 5
utils/reel_generator.py converting 2D news cards into 1080x1920 9:16 vertical video reels.

facebook/reels_publisher.py handling Meta's 3-step video reel upload protocol.

Updated job_queue/worker.py posting 1 Image Post + 1 Reel every 4 minutes.

test_entire_system.py providing a dry-run check of the pipeline.